{
  "name": "prism",
  "version": "2.5.0",
  "description": "Prism — local-first spending tracker with bank sync",
  "main": "electron/main.js",
  "scripts": {
    "start": "node server.js",
    "dev": "electron .",
    "test": "node --test tests/*.test.js",
    "generate-icons": "node scripts/generate-icons.js",
    "write-version": "node scripts/write-version.js",
    "build:win": "electron-builder --win",
    "build:mac": "electron-builder --mac",
    "build:all": "electron-builder --win --mac"
  },
  "build": {
    "appId": "com.prism.app",
    "productName": "Prism",
    "directories": {
      "output": "dist"
    },
    "files": [
      "electron/**/*",
      "server.js",
      "src/**/*",
      "public/**/*",
      "!data/**/*",
      "!dist/**/*",
      "!scripts/**/*",
      "!tests/**/*"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "build/icon.ico"
    },
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": [
            "x64",
            "arm64"
          ]
        }
      ],
      "icon": "build/icon.icns",
      "category": "public.app-category.finance"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "artifactName": "Prism-Setup.exe"
    },
    "dmg": {
      "title": "Prism",
      "artifactName": "Prism-${arch}.dmg"
    }
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.90.0",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "electron": "^44.4.4",
    "electron-builder": "^26.15.3"
  }
}
